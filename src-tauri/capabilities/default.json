{
  "identifier": "default",
  "description": "Default capabilities for SysForge",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "fs:allow-read",
    "fs:allow-write",
    {
      "identifier": "fs:scope",
      "allow": [
        { "path": "$APPDATA/sysforge/**" }
      ]
    }
  ]
}